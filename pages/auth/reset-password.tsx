import ResetPasswordContainer from 'src/modules/auth/components/ResetPasswordContainer';

export const resetPassword = (): JSX.Element => (
  <>
    <ResetPasswordContainer />
  </>
);

export default resetPassword;
